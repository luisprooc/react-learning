export const sum = (a,b) => a + b;

export const rest = (a,b) => a - b;

export const mult = (a,b) => a * b;

export const getRandomNum = (min,max) => Math.ceil(Math.random() * ( max - min + 1) + min);